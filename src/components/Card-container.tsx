"use client"

import { useEffect } from "react";
import {
  getPlatosPrincipales,
  getLomitos,
  getSalsas,
} from "../server/loadData.js";

const CardContainer = () => {
//   useEffect(() => {
//     // Ejemplo de cómo usar las funciones en el componente
//     const loadData = async () => {
//       const platosPrincipales = await getPlatosPrincipales();
//       console.log("Platos Principales:", platosPrincipales);

//       const salsas = await getSalsas();
//       console.log("Salsas:", salsas);

//       const lomitos = await getLomitos();
//       console.log("Lomitos:", lomitos);
//     };

//     loadData();
//   }, []);

  return (<>
  
  </>);
};

export default CardContainer;
